I want you to make webserver in js. I want to have a clinet page (index.html), that has a question as heading and then has a text area and a send button. On send button click I want the answer to be shown on the output.htlnl as a list. use bun std lib


-- 
make server
input output

first text

set interval

---

canvas 
draw canvas and display drawings

add a select button either draw or answer with text


esp can not handle that many things at the same time and also cannot handle storing images.

also realistically the esp and the projection cannot be on the same ip

the esp can be only for the ligths??/ 

choose a blob storage
either vercel or firebase

with firebase we can also do realtime i guess


I want when 10 answers are submitted 
then the esp makes a get request and the sequesnce should start
and it controlls the leds internally

probabaly keep a global state 


make a separate page that has a sequence of 



@@@@@@@@@ 
make also the loadgin responsible, not just pooping from noting


i want to deploy to vercel beacsue they have vercel blob

bun only is not supported
have to use hono